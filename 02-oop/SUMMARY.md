# 02 — Object-Oriented Programming (OOP)

## Nội dung

- Encapsulation — đóng gói
- Inheritance — kế thừa
- Polymorphism — đa hình
- Abstraction — trừu tượng
- Composition vs Inheritance — khi nào nên dùng composition thay vì inheritance

## Cấu trúc

```text
02-oop/
├── README.md
├── COMPLETION.md
├── encapsulation/
│   └── BankAccount.java
├── inheritance/
│   └── Employee.java
├── polymorphism/
│   └── Animal.java
├── abstraction/
│   └── Payment.java
└── composition-vs-inheritance/
    └── Vehicle.java
```

Mỗi thư mục chủ đề chỉ chứa đúng một file Java như cấu trúc yêu cầu.

## Yêu cầu môi trường

- Java JDK 17 trở lên

## Tiêu chuẩn code

Các ví dụ được thiết kế để:

- chạy độc lập;
- có `main()` để kiểm tra kết quả trực tiếp;
- không cần thư viện bên ngoài;
- không dùng `@SuppressWarnings` để che warning;
- minh họa đúng bản chất của từng khái niệm OOP.

